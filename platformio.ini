[platformio]
src_dir = Dumb_Little_Robot_V1

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
lib_deps =
    adafruit/Adafruit GFX Library @ ^1.11.5
    adafruit/Adafruit SSD1306 @ ^2.4.7
    https://github.com/me-no-dev/ESPAsyncWebServer.git
    https://github.com/me-no-dev/AsyncTCP.git
    ; Use the local library in lib/ (ESP32_AnalogWrite was unpacked there)

monitor_speed = 115200

lib_compat_mode = strict ; necessary if you are using PlatformIO to avoid auto detection from PlatformIO and loading standard WebServer library
lib_ignore = WebServer 
